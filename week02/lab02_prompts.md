# Lab 02 CLI comparison journal

Do not include passwords, tokens, API keys, or complete authentication output.

## Tool check

### GitHub Copilot CLI

I have installed and authenticated GitHub Copilot CLI, the version is v1.0.82.

### Antigravity CLI

I have installed and authenticated Antigravity CLI, the version is v1.1.24.

## Shared task

### Shared prompt

Paste the exact prompt you submitted to both CLI tools.

```text
Finish the function based on its individual comment
```

### Copilot CLI observations

Copilot CLI suggested a sum function that adds up characters in text.lower in a list of aieou. I questioned this approach because it was slightly different than how the VS Code Copilot generated the other two functions. I verified that this function worked by running the function and testing the output. Overall, the Copilot CLI did its job correctly, but not as fast as the VS Code Copilot.

### Antigravity CLI observations

The first thing I noticed about Antigravity is that it would scan through each document in the folder to find what the prompt was looking for. Then, it suggested a very similar function that the Copilot CLI suggested. I tested the output of the Antigravity version of the function as well, and it worked. I questioned why Antigravity had to scan through each document in the folder instead of knowing which document I was on. Overall, it did its job correctly, but not extremely efficiently.

### Comparison

Both the GitHub Copilot CLI and the Antigravity CLI correctly generated accurate code for the function. However, the Copilot CLI was much more consise and had a much more simple response than the Antigravity CLI. This made it much easier to understand and it had much more clarity. The Copilot CLI assumed which document I was in and didn't need to scan through all of the documents in order to find it. I approached this by asking each CLI to fill in the fuction based on the comment, and while they both did so correctly, I would say that the Copilot CLI is more useful because it has more clarity and speed.

## Test-guided implementation

In order to test both the GitHub Copilot and Antigravity CLI outputs, I ran each function and manually verified that each output was correct. Both of the CLIs gave me a very similar function with only minor differences, so they worked mostly the same. The sum of the number of vowels matched the text entered, so they both ended up working fine. Since they were both similar functions, I just picked the Copilot generated function because it was more consise and easier to understand. No modifications to the function had to be made because the function was working right after being generated by the Copilot CLI.

## Preferred tool combination

For me, I found that the VS Code Copilot browser chat was the most effective for the task. It is built right into VS Code and it makes it very convenient to access and is very easy to use. The sidebar allows you to ask Copilot anything you want while also keeping your terminal open. It worked just as well as the Antigravity and GitHub Copilot CLIs and was much faster than the both of them, not only to setup, but also to generate the code. In most situations, I imagine that I would use the browser chat if I needed help with my code. However, I can see a situation where there could be a very complex project that I am working on where the Github Copilot CLI could be useful to go through and help me with that type of code and provide guidance to help me understand what it did. Personally, I did not like Antigravity, so that would be my last choice.
